#include <stdio.h>

/*int main(){

    int age; //variable declaration
    printf("Enter your Age :");
    scanf("%d",&age); //taking user input for variable usage
    (age >= 18)? (printf("You are Eligible for voting ( * _ * )")): (printf("You are NOT eligible for voting")); //conditional operator
    return 0;
}*/

int main(){
    int alex = 17,delei;
    delei = ((alex==18)? (18): (19));

    printf("My age is : %d",delei);
    return 0;
     return 0;
}